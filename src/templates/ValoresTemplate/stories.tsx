import { Story, Meta } from '@storybook/react'
import ValoresTemplate from '.'

export default {
  title: 'ValoresTemplate',
  component: ValoresTemplate
} as Meta

export const Default: Story = () => <ValoresTemplate />
