import { Story, Meta } from '@storybook/react'
import InfoTemplate from '.'

export default {
  title: 'InfoTemplate',
  component: InfoTemplate
} as Meta

export const Default: Story = () => <InfoTemplate />
