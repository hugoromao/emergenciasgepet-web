import { Story, Meta } from '@storybook/react'
import MeusArtigosTemplate from '.'

export default {
  title: 'MeusArtigosTemplate',
  component: MeusArtigosTemplate
} as Meta

export const Default: Story = () => <MeusArtigosTemplate />
