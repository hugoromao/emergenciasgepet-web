import { Story, Meta } from '@storybook/react'
import ResetPasswordForm from '.'

export default {
  title: 'ResetPasswordForm',
  component: ResetPasswordForm
} as Meta

export const Default: Story = () => <ResetPasswordForm />
