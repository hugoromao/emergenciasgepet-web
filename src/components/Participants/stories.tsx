import { Story, Meta } from '@storybook/react'
import Participants from '.'

export default {
  title: 'Participants',
  component: Participants
} as Meta

export const Default: Story = () => <Participants />
