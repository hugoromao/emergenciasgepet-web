import { Story, Meta } from '@storybook/react'
import ScheduleSection from '.'

export default {
  title: 'ScheduleSection',
  component: ScheduleSection
} as Meta

export const Default: Story = () => <ScheduleSection />
