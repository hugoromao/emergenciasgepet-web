import styled from 'styled-components'

export const Wrapper = styled.div`
  display: flex;
  flex-direction: column;

  button {
    position: absolute;
    bottom: 2.4rem;
    right: 2.4rem;
  }

  h1 {
    font-size: 1.6rem;
  }
`
